import 'package:flutter/material.dart';

/// Stub helper to capture a photo from webcam on the Web.
/// Returns null on mobile platforms so we fall back to ImagePicker.
Future<String?> captureWebcamPhoto(BuildContext context) async {
  return null;
}
